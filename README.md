> **Note:** `autoprefixer` and `@headlessui/react` are pinned to `10.4.20` and `1.7.19` respectively to avoid Windows npm `ETARGET` errors and keep install reliability without drifting into breaking API changes.

